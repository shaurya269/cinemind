# Copy to .env.local for local dev, or set as a Vercel project env var
# (Project Settings -> Environment Variables) for the deployed site.
# Points the React app at the FastAPI backend -- e.g. the Render service
# from render.yaml, such as https://cinemind-api.onrender.com
VITE_API_BASE=http://localhost:8000
