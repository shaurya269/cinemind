# Data files - too large for git, download separately (see SETUP_GUIDE.md)
data/ml-100k/
data/*.csv

# Trained model artifacts - regenerated by running the scripts
artifacts/

# Secrets - never commit API keys
.env

# Backend local fallback state (used when Postgres isn't running)
backend/feedback.db
streamlit_app/*.log

# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# OS / editor
.DS_Store
.vscode/
.idea/
*.ipynb_checkpoints

# Frontend (Phase 3)
frontend/node_modules/
frontend/dist/
